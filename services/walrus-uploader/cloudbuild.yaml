steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - services/walrus-uploader/Dockerfile
      - -t
      - ${_IMAGE}
      - .
images:
  - ${_IMAGE}
substitutions:
  _IMAGE: us-central1-docker.pkg.dev/$PROJECT_ID/clawnews/walrus-uploader:staging
